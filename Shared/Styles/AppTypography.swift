import SwiftUI

extension Font {
    static let structureTitle = Font.system(.title2, design: .rounded, weight: .semibold)
    static let structureSubtitle = Font.system(.subheadline, design: .rounded, weight: .medium)
    static let bodyText = Font.system(.body, design: .default)
    static let regionBadge = Font.system(.caption, design: .rounded, weight: .bold)
    static let quizPrompt = Font.system(.title3, design: .rounded, weight: .medium)
    static let scoreDisplay = Font.system(.largeTitle, design: .rounded, weight: .bold)
    static let sectionHeader = Font.system(.headline, design: .rounded, weight: .semibold)
    static let detailLabel = Font.system(.caption, design: .rounded, weight: .medium)
}
